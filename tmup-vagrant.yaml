session_name: vagrant-dev
windows:
- window_name: dev window
  layout: tiled
  shell_command_before:
    - cd ~/                    # run as a first command in all panes
  panes:
    - shell_command:           # pane no. 1
        - cd ~/vms          # run multiple commands in this pane
        - ls -al
    - echo second pane         # pane no. 2
